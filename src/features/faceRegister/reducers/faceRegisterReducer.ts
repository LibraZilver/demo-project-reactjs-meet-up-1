import { AnyAction } from 'redux'

const faceRegisterInitState = {}

export const faceRegisterReducer = (state = faceRegisterInitState, action: AnyAction) => {
  switch (action.type) {
    default:
      return state
  }
}
